import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['images.pexels.com', 'www.pexels.com', 'img.clerk.com', 'res.cloudinary.com'],
  },
}

export default nextConfig;
